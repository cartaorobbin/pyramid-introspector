{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pyramid-introspector","text":"<p>Extract route and view metadata from Pyramid applications, with an extension system that handles library-specific introspection for Cornice and pycornmarsh.</p>"},{"location":"#why-this-library","title":"Why this library?","text":"<p>Pyramid's introspectable registry holds rich metadata about routes and views, but extracting it -- especially when libraries like Cornice and pycornmarsh layer their own conventions on top -- requires non-trivial, repetitive code. This library provides a single, tested entry point for that work.</p>"},{"location":"#key-features","title":"Key features","text":"<ul> <li>Typed dataclass output -- <code>RouteInfo</code> and <code>ViewInfo</code> objects with clear, documented fields</li> <li>Extension pipeline -- built-in support for Cornice and pycornmarsh, plus a protocol for custom extensions</li> <li>Schema extraction -- Marshmallow request/response schemas are captured as <code>SchemaInfo</code> objects with field-level detail</li> <li>Permission and security metadata -- extracted from Pyramid ACL and pycornmarsh predicates</li> <li>Auto-discovery -- extensions are loaded automatically when their dependencies are installed, or registered via entry points</li> <li>Zero configuration -- pass a Pyramid registry, call <code>.introspect()</code>, done</li> </ul>"},{"location":"#quick-look","title":"Quick look","text":"<pre><code>from pyramid_introspector import PyramidIntrospector\n\nintrospector = PyramidIntrospector(config.registry)\nroutes = introspector.introspect()\n\nfor route in routes:\n    for view in route.views:\n        print(f\"{view.method:6} {route.pattern}\")\n</code></pre>"},{"location":"#next-steps","title":"Next steps","text":"<ul> <li>Getting Started -- install the library and run your first introspection</li> <li>Usage Guide -- explore the data model, work with schemas, and filter routes</li> <li>Extensions -- understand built-in extensions and write your own</li> <li>API Reference -- complete reference for all public classes and functions</li> </ul>"},{"location":"api-reference/","title":"API Reference","text":""},{"location":"api-reference/#introspector","title":"Introspector","text":""},{"location":"api-reference/#pyramid_introspector.introspector.PyramidIntrospector","title":"PyramidIntrospector","text":"<p>Introspect a Pyramid application's routes and views.</p> <p>Discovers routes from Pyramid's introspectable registry and enriches them through a chain of extensions that handle library-specific metadata (Cornice services, pycornmarsh predicates, etc.).</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>Any</code> <p>A committed Pyramid registry.</p> required <code>extensions</code> <code>list[IntrospectionExtension] | None</code> <p>Optional list of extensions. When None, extensions are auto-discovered from built-ins and entry points.</p> <code>None</code>"},{"location":"api-reference/#pyramid_introspector.introspector.PyramidIntrospector.introspect","title":"introspect","text":"<pre><code>introspect() -&gt; list[RouteInfo]\n</code></pre> <p>Discover routes and enrich them through all available extensions.</p> <p>Returns:</p> Type Description <code>list[RouteInfo]</code> <p>List of RouteInfo objects, each containing ViewInfo objects</p> <code>list[RouteInfo]</code> <p>enriched with library-specific metadata.</p>"},{"location":"api-reference/#data-models","title":"Data Models","text":""},{"location":"api-reference/#pyramid_introspector.models.RouteInfo","title":"RouteInfo  <code>dataclass</code>","text":"<p>A route discovered from a Pyramid application, grouping its views.</p>"},{"location":"api-reference/#pyramid_introspector.models.ViewInfo","title":"ViewInfo  <code>dataclass</code>","text":"<p>A single view associated with a route, representing one HTTP method handler.</p>"},{"location":"api-reference/#pyramid_introspector.models.ParameterInfo","title":"ParameterInfo  <code>dataclass</code>","text":"<p>A single parameter extracted from a route pattern or schema.</p>"},{"location":"api-reference/#pyramid_introspector.models.SchemaInfo","title":"SchemaInfo  <code>dataclass</code>","text":"<p>A Marshmallow schema captured from introspection.</p>"},{"location":"api-reference/#pyramid_introspector.models.SchemaFieldInfo","title":"SchemaFieldInfo  <code>dataclass</code>","text":"<p>A single field in a Marshmallow schema.</p>"},{"location":"api-reference/#discovery","title":"Discovery","text":""},{"location":"api-reference/#pyramid_introspector.discovery.discover_routes","title":"discover_routes","text":"<pre><code>discover_routes(registry: Any) -&gt; list[RouteInfo]\n</code></pre> <p>Discover routes and views from a Pyramid registry.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>Any</code> <p>A committed Pyramid registry (from Configurator.registry after make_wsgi_app, or from pyramid.paster.bootstrap).</p> required <p>Returns:</p> Type Description <code>list[RouteInfo]</code> <p>List of RouteInfo with one ViewInfo per route+method combination.</p>"},{"location":"api-reference/#extension-system","title":"Extension System","text":""},{"location":"api-reference/#pyramid_introspector.extensions.IntrospectionExtension","title":"IntrospectionExtension","text":"<p>               Bases: <code>Protocol</code></p> <p>Protocol that all introspection extensions must satisfy.</p>"},{"location":"api-reference/#pyramid_introspector.extensions.IntrospectionExtension.is_available","title":"is_available","text":"<pre><code>is_available() -&gt; bool\n</code></pre> <p>Return True if the library this extension handles is installed.</p>"},{"location":"api-reference/#pyramid_introspector.extensions.IntrospectionExtension.enrich","title":"enrich","text":"<pre><code>enrich(\n    registry: Any, routes: list[RouteInfo]\n) -&gt; list[RouteInfo]\n</code></pre> <p>Enrich route info with library-specific metadata.</p> <p>Extensions should mutate or replace the ViewInfo objects in the provided routes, adding parameters, schemas, and extra metadata.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>Any</code> <p>The Pyramid registry.</p> required <code>routes</code> <code>list[RouteInfo]</code> <p>Routes discovered so far (may already be enriched by earlier extensions).</p> required <p>Returns:</p> Type Description <code>list[RouteInfo]</code> <p>The enriched list of routes.</p>"},{"location":"api-reference/#pyramid_introspector.extensions.auto_discover_extensions","title":"auto_discover_extensions","text":"<pre><code>auto_discover_extensions() -&gt; list[IntrospectionExtension]\n</code></pre> <p>Discover and instantiate extensions from entry points and built-ins.</p> <p>Built-in extensions (cornice, pycornmarsh) are always included when their dependencies are available.  Additional extensions can be registered via the <code>pyramid_introspector.extensions</code> entry point group.</p>"},{"location":"extensions/","title":"Extensions","text":"<p>Extensions enrich the base route and view metadata with library-specific information. They form a pipeline: each extension receives the routes produced so far and returns an enriched version.</p>"},{"location":"extensions/#how-extensions-work","title":"How extensions work","text":"<pre><code>flowchart LR\n    A[Pyramid Registry] --&gt; B[discover_routes]\n    B --&gt; C[CorniceExtension]\n    C --&gt; D[PycornmarshExtension]\n    D --&gt; E[\"list[RouteInfo]\"]</code></pre> <ol> <li><code>discover_routes</code> reads Pyramid's introspectable registry and produces a base <code>list[RouteInfo]</code> with path parameters, HTTP methods, permissions, and docstrings.</li> <li>Each extension's <code>is_available()</code> is checked -- if the extension's dependency is not installed, it is skipped.</li> <li>Each available extension's <code>enrich()</code> receives the registry and the current route list, and returns an updated list.</li> </ol> <p>Extensions are run in order. The built-in Cornice extension runs before pycornmarsh, because pycornmarsh reads data that Cornice stores in <code>view.extra[\"cornice_args\"]</code>.</p>"},{"location":"extensions/#built-in-extensions","title":"Built-in extensions","text":""},{"location":"extensions/#cornice","title":"Cornice","text":"<p>Activated when: <code>cornice</code> is installed.</p> <p>The Cornice extension discovers Cornice services, matches them to routes by path pattern, and extracts Marshmallow schema metadata from service definitions.</p> <p>It handles two schema patterns:</p> <p>Composite schemas -- top-level <code>Nested</code> fields named <code>body</code>, <code>querystring</code>, or <code>path</code>:</p> <pre><code>import marshmallow as ma\n\nclass BodySchema(ma.Schema):\n    name = ma.fields.String(required=True)\n\nclass QuerySchema(ma.Schema):\n    page = ma.fields.Integer()\n\nclass MyRequestSchema(ma.Schema):\n    body = ma.fields.Nested(BodySchema)\n    querystring = ma.fields.Nested(QuerySchema)\n</code></pre> <p>Flat schemas -- a single schema where the location is inferred from the Cornice validator function:</p> <pre><code>@service.post(schema=BodySchema, validators=(colander_body_validator,))\ndef create_item(request):\n    ...\n</code></pre> <p>The extension also extracts response schemas from <code>response_schema</code> attributes on view callables.</p> <p>What gets populated:</p> ViewInfo field Source <code>parameters</code> Schema fields as <code>ParameterInfo</code> objects <code>request_schema</code> Body schema as <code>SchemaInfo</code> <code>querystring_schema</code> Querystring schema as <code>SchemaInfo</code> <code>response_schema</code> <code>view_callable.response_schema</code> <code>description</code> Cornice service description (if view has none) <code>extra[\"cornice_service_name\"]</code> Service name <code>extra[\"cornice_args\"]</code> Raw Cornice definition args dict"},{"location":"extensions/#pycornmarsh","title":"pycornmarsh","text":"<p>Activated when: <code>cornice</code> is installed (pycornmarsh builds on Cornice).</p> <p>The pycornmarsh extension reads pycornmarsh-specific predicates from the Cornice service definition args (stored by the Cornice extension in <code>view.extra[\"cornice_args\"]</code>).</p> <p>Recognized predicates:</p> Predicate ViewInfo field / extra key Description <code>pcm_request</code> <code>parameters</code>, <code>request_schema</code>, <code>querystring_schema</code> Schema dict by location <code>pcm_responses</code> <code>response_schema</code>, <code>response_schemas</code> Schemas keyed by status code <code>pcm_security</code> <code>security</code>, <code>extra[\"pcm_security\"]</code> Auth scheme type <code>pcm_tags</code> <code>extra[\"pcm_tags\"]</code> API tags <code>pcm_summary</code> <code>extra[\"pcm_summary\"]</code> Endpoint summary <code>pcm_description</code> <code>description</code>, <code>extra[\"pcm_description\"]</code> Endpoint description <code>pcm_show</code> <code>extra[\"pcm_show\"]</code> Visibility flag <p>Extension ordering</p> <p>The pycornmarsh extension depends on the Cornice extension having already run. The built-in auto-discovery guarantees this ordering. If you supply extensions manually, make sure Cornice comes first.</p>"},{"location":"extensions/#writing-a-custom-extension","title":"Writing a custom extension","text":"<p>Any class that satisfies the <code>IntrospectionExtension</code> protocol can be used as an extension:</p> <pre><code>from pyramid_introspector.extensions import IntrospectionExtension\nfrom pyramid_introspector.models import RouteInfo\n\nclass MyExtension:\n    name = \"my_library\"\n\n    def is_available(self) -&gt; bool:\n        try:\n            import my_library\n            return True\n        except ImportError:\n            return False\n\n    def enrich(self, registry, routes: list[RouteInfo]) -&gt; list[RouteInfo]:\n        for route in routes:\n            for view in route.views:\n                view.extra[\"my_data\"] = \"enriched\"\n        return routes\n</code></pre> <p>The protocol requires:</p> <ul> <li><code>name</code> (<code>str</code>) -- a human-readable identifier for logging</li> <li><code>is_available()</code> -- return <code>True</code> if this extension's dependencies are installed</li> <li><code>enrich(registry, routes)</code> -- receive the Pyramid registry and current route list, return the enriched list</li> </ul>"},{"location":"extensions/#passing-extensions-manually","title":"Passing extensions manually","text":"<pre><code>from pyramid_introspector import PyramidIntrospector\n\nintrospector = PyramidIntrospector(\n    registry,\n    extensions=[MyExtension()],\n)\nroutes = introspector.introspect()\n</code></pre> <p>Warning</p> <p>When you pass extensions manually, auto-discovery is disabled entirely. If you still want the built-in extensions, include them explicitly:</p> <pre><code>from pyramid_introspector.extensions.cornice import CorniceExtension\nfrom pyramid_introspector.extensions.pycornmarsh import PycornmarshExtension\n\nintrospector = PyramidIntrospector(\n    registry,\n    extensions=[CorniceExtension(), PycornmarshExtension(), MyExtension()],\n)\n</code></pre>"},{"location":"extensions/#registering-via-entry-points","title":"Registering via entry points","text":"<p>For automatic discovery, register your extension class as an entry point in your package's <code>pyproject.toml</code>:</p> <pre><code>[project.entry-points.\"pyramid_introspector.extensions\"]\nmy_library = \"my_package.introspection:MyExtension\"\n</code></pre> <p>The class will be imported, instantiated (with no arguments), and checked against the <code>IntrospectionExtension</code> protocol at runtime. If it satisfies the protocol, it is added to the extension pipeline after the built-in extensions.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.12 or later</li> <li>A Pyramid application with a committed registry</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":"BaseWith Cornice support <pre><code>pip install pyramid-introspector\n</code></pre> <pre><code>pip install pyramid-introspector[cornice]\n</code></pre> <p>This pulls in <code>cornice</code> and <code>marshmallow</code>, enabling the built-in Cornice and pycornmarsh extensions.</p>"},{"location":"getting-started/#quick-start","title":"Quick start","text":"<p>The entry point is <code>PyramidIntrospector</code>. Pass it a committed Pyramid registry and call <code>introspect()</code>:</p> <pre><code>from pyramid.config import Configurator\nfrom pyramid_introspector import PyramidIntrospector\n\nconfig = Configurator()\n# ... add routes, views, include cornice, etc.\nconfig.commit()\n\nintrospector = PyramidIntrospector(config.registry)\nroutes = introspector.introspect()\n\nfor route in routes:\n    for view in route.views:\n        print(f\"{view.method:6} {route.pattern}\")\n        if view.request_schema:\n            print(f\"       body: {view.request_schema.name}\")\n        if view.response_schema:\n            print(f\"       resp: {view.response_schema.name}\")\n</code></pre> <p>Using paster/plaster</p> <p>If your application is configured via an INI file, you can bootstrap the registry like this:</p> <pre><code>from pyramid.paster import bootstrap\n\nenv = bootstrap(\"production.ini\")\nregistry = env[\"registry\"]\n\nintrospector = PyramidIntrospector(registry)\nroutes = introspector.introspect()\n\nenv[\"closer\"]()\n</code></pre>"},{"location":"getting-started/#what-comes-back","title":"What comes back","text":"<p><code>introspect()</code> returns a <code>list[RouteInfo]</code>. Each <code>RouteInfo</code> holds the route name, URL pattern, and a list of <code>ViewInfo</code> objects -- one per HTTP method handler. See the Usage Guide for a detailed walkthrough of the data model.</p>"},{"location":"usage/","title":"Usage Guide","text":"<p>This guide walks through the data structures returned by <code>PyramidIntrospector</code> and shows common patterns for working with them.</p>"},{"location":"usage/#the-data-model","title":"The data model","text":"<pre><code>classDiagram\n    RouteInfo \"1\" --&gt; \"*\" ViewInfo : views\n    ViewInfo \"1\" --&gt; \"*\" ParameterInfo : parameters\n    ViewInfo \"1\" --&gt; \"0..1\" SchemaInfo : request_schema\n    ViewInfo \"1\" --&gt; \"0..1\" SchemaInfo : querystring_schema\n    ViewInfo \"1\" --&gt; \"0..1\" SchemaInfo : response_schema\n    ViewInfo \"1\" --&gt; \"*\" SchemaInfo : response_schemas\n    SchemaInfo \"1\" --&gt; \"*\" SchemaFieldInfo : fields\n\n    class RouteInfo {\n        +str name\n        +str pattern\n        +list~ViewInfo~ views\n        +Any factory\n        +dict extra\n    }\n\n    class ViewInfo {\n        +str method\n        +Any callable\n        +str|None permission\n        +str|None security\n        +str description\n        +list~ParameterInfo~ parameters\n        +SchemaInfo|None request_schema\n        +SchemaInfo|None querystring_schema\n        +SchemaInfo|None response_schema\n        +dict~int,SchemaInfo~ response_schemas\n        +dict extra\n    }\n\n    class ParameterInfo {\n        +str name\n        +str location\n        +bool required\n        +str type_hint\n        +str description\n    }\n\n    class SchemaInfo {\n        +str name\n        +list~SchemaFieldInfo~ fields\n    }\n\n    class SchemaFieldInfo {\n        +str name\n        +str field_type\n        +bool required\n        +dict metadata\n    }</code></pre>"},{"location":"usage/#routeinfo","title":"RouteInfo","text":"<p>Each <code>RouteInfo</code> represents a single Pyramid route:</p> Field Type Description <code>name</code> <code>str</code> Pyramid route name <code>pattern</code> <code>str</code> URL pattern (e.g. <code>/items/{id}</code>) <code>views</code> <code>list[ViewInfo]</code> One entry per HTTP method <code>factory</code> <code>Any</code> Route factory for ACL contexts <code>extra</code> <code>dict</code> Extension-specific route metadata"},{"location":"usage/#viewinfo","title":"ViewInfo","text":"<p>Each <code>ViewInfo</code> represents one HTTP method handler on a route:</p> Field Type Description <code>method</code> <code>str</code> HTTP method (<code>GET</code>, <code>POST</code>, ...) <code>callable</code> <code>Any</code> The Python view function <code>permission</code> <code>str \\| None</code> Pyramid permission (from ACL) <code>security</code> <code>str \\| None</code> Auth scheme (e.g. <code>\"bearer\"</code>, <code>\"BearerAuth\"</code>) <code>description</code> <code>str</code> First line of view docstring <code>parameters</code> <code>list[ParameterInfo]</code> Path, querystring, and body parameters <code>request_schema</code> <code>SchemaInfo \\| None</code> Marshmallow schema for request body <code>querystring_schema</code> <code>SchemaInfo \\| None</code> Marshmallow schema for querystring <code>response_schema</code> <code>SchemaInfo \\| None</code> Primary 2xx response schema <code>response_schemas</code> <code>dict[int, SchemaInfo]</code> All response schemas keyed by status code <code>extra</code> <code>dict</code> Extension-specific view metadata <p><code>ViewInfo</code> also provides convenience properties for filtering parameters by location:</p> <pre><code>view.path_parameters        # parameters where location == \"path\"\nview.querystring_parameters # parameters where location == \"querystring\"\nview.body_parameters        # parameters where location == \"body\"\nview.has_body               # True if any body parameters exist\n</code></pre>"},{"location":"usage/#working-with-parameters","title":"Working with parameters","text":"<p>Parameters are extracted from route patterns (path parameters) and from Marshmallow schemas (querystring and body parameters, when Cornice/pycornmarsh is used):</p> <pre><code>for route in routes:\n    for view in route.views:\n        for param in view.path_parameters:\n            print(f\"  path: {param.name} ({param.type_hint})\")\n\n        for param in view.querystring_parameters:\n            print(f\"  qs:   {param.name} ({param.type_hint}, required={param.required})\")\n\n        for param in view.body_parameters:\n            print(f\"  body: {param.name} ({param.type_hint})\")\n</code></pre>"},{"location":"usage/#working-with-schemas","title":"Working with schemas","text":"<p>When Cornice or pycornmarsh extensions are active, views may carry Marshmallow schema information:</p> <pre><code>for route in routes:\n    for view in route.views:\n        if view.request_schema:\n            print(f\"Request body schema: {view.request_schema.name}\")\n            for field in view.request_schema.fields:\n                print(f\"  {field.name}: {field.field_type} (required={field.required})\")\n\n        if view.querystring_schema:\n            print(f\"Querystring schema: {view.querystring_schema.name}\")\n\n        if view.response_schema:\n            print(f\"Response schema: {view.response_schema.name}\")\n\n        for status_code, schema in view.response_schemas.items():\n            print(f\"  {status_code}: {schema.name}\")\n</code></pre>"},{"location":"usage/#permissions-and-security","title":"Permissions and security","text":"<p>Permissions are extracted from Pyramid's ACL system. Security metadata comes from pycornmarsh's <code>pcm_security</code> predicate:</p> <pre><code>secured_views = [\n    (route, view)\n    for route in routes\n    for view in route.views\n    if view.permission\n]\n\nfor route, view in secured_views:\n    print(f\"{view.method} {route.pattern} requires '{view.permission}'\")\n    if view.security:\n        print(f\"  auth scheme: {view.security}\")\n</code></pre>"},{"location":"usage/#the-extra-dict","title":"The extra dict","text":"<p>Extensions can store arbitrary metadata in <code>route.extra</code> and <code>view.extra</code>. The Cornice extension stores:</p> <ul> <li><code>view.extra[\"cornice_service_name\"]</code> -- the Cornice service name</li> <li><code>view.extra[\"cornice_args\"]</code> -- the raw Cornice definition args</li> </ul> <p>The pycornmarsh extension stores:</p> <ul> <li><code>view.extra[\"pcm_tags\"]</code> -- API tags</li> <li><code>view.extra[\"pcm_summary\"]</code> -- endpoint summary</li> <li><code>view.extra[\"pcm_description\"]</code> -- endpoint description</li> <li><code>view.extra[\"pcm_security\"]</code> -- security scheme</li> <li><code>view.extra[\"pcm_show\"]</code> -- visibility flag</li> </ul> <pre><code>for route in routes:\n    for view in route.views:\n        tags = view.extra.get(\"pcm_tags\", [])\n        if tags:\n            print(f\"{view.method} {route.pattern} tags: {tags}\")\n</code></pre>"},{"location":"usage/#filtering-routes","title":"Filtering routes","text":"<p>Since routes are plain dataclasses in a list, standard Python filtering works:</p> <pre><code>api_routes = [r for r in routes if r.pattern.startswith(\"/api/\")]\n\npost_views = [\n    (route, view)\n    for route in routes\n    for view in route.views\n    if view.method == \"POST\"\n]\n\nroutes_with_schemas = [\n    route for route in routes\n    if any(v.request_schema for v in route.views)\n]\n</code></pre>"}]}