[tox]
requires = tox-uv
env_list = py310, py311, py312, py313, lint

[testenv]
deps =
    pytest
    webtest
    cornice
    marshmallow
commands =
    pytest {posargs}

[testenv:lint]
deps =
    ruff
    black
commands =
    ruff check .
    black --check .
